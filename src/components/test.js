import React from 'react';

function Test(props) {
    return (
        <div>

            <div>
                <strong>{props.user.name}</strong>
            </div>
        </div>
    );
}
export default Test;